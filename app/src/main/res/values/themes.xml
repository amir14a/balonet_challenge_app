<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.BalonetChallengeApplication" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/appPrimaryColor</item>
        <item name="android:layoutDirection">rtl</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="Theme.BalonetChallengeApplication" parent="Base.Theme.BalonetChallengeApplication" />
</resources>